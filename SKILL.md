---
name: news2-deterioration-alert
description: 当护士提供成人住院患者的一次生命体征测量（呼吸频率、SpO₂、是否吸氧、收缩压、心率、意识 ACVPU、体温），需要按 NEWS2 评分判断病情恶化风险、给出报警级别与监测频率，或需要生成上报用的 SBAR 草稿时使用。也用于用户提到"早期预警评分""NEWS2""要不要上报医生""病情是不是在恶化"等情境。仅用于普通病房 16 岁及以上非妊娠患者的筛查辅助；不做诊断、不给治疗建议、不修改医嘱，所有输出须由护士确认。
---

# NEWS2 病情早期恶化报警（V2）

> V2 基于 V1 边界测试的失败证据修订，修改项见文末「版本记录」。

## 教学声明

本 Skill 为课程教学作品，仅供学习与演示，**不得用于真实临床决策**。所有示例病例与数据均为虚构。

## 目标

帮助普通病房的责任护士，及时识别需要加密监测和上报的成人住院患者：按 RCP NEWS2 对 7 个评分项逐项评分、给出有依据的报警级别，并结构化呈现缺失数据、不确定性和 SBAR 上报草稿。

## 依据（Grounding）

评分阈值、报警级别和临床响应**只能**读取 `references/news2-scoring.md`，不得凭模型记忆修改阈值，不得引入其他评分体系（MEWS、qSOFA 等）。每个结论后注明依据来源。

## 工作流程（6 步，不得跳步）

### 第 1 步：确认适用范围

- 不适用者**不评分**，输出范围外提示并建议按医院流程处理：
  - 16 岁以下儿童 → 提示使用儿童专用评分
  - 妊娠或产后患者 → 提示使用产科专用评分
  - 医嘱规定使用 SpO₂ 量表 2 或目标血氧 88%–92%（如 COPD 伴高碳酸血症风险）→ 提示本 Skill 不支持量表 2
- 未提供年龄或妊娠信息时，按成人非妊娠处理，并在「缺失与不确定性」中注明这一假设。

### 第 2 步：核对 7 个评分项

7 项 = 呼吸频率、SpO₂、是否吸氧、收缩压、心率、意识（ACVPU）、体温。

- **有任一缺项 → 不计算总分**，列出缺失项并提示补测。严禁按 0 分计算或自行估计。
- 数值明显不合理（如心率 400 次/分、体温 60 ℃）→ 不评分，提示核对并复测。
- 描述模糊无法归类（如"精神不太好"）→ 标为「需要护士确认」，不猜测 ACVPU 等级。

### 第 3 步：逐项评分

- **优先调用** `scripts/news2_score.py` 计算，减少算术错误。
- 无法运行脚本时，按 `references/news2-scoring.md` 的评分表人工计算，并逐项列出得分供护士复核。
- 标出所有得 3 分的单项。
- **只有真正调用了脚本，才能描述脚本的返回结果。** 未调用时不得写"脚本返回 X""脚本试算显示 Y"等内容——这属于编造工具调用记录。第 1、2 步判定为范围外或缺项时本就不会调用脚本，此时一律不提脚本。

### 第 4 步：判断报警级别

按以下顺序判定，**命中即停**：

1. 总分 ≥ 7 → 高
2. 总分 5–6 → 中
3. 任一单项 3 分且总分 < 5 → 低–中
4. 总分 1–4 → 低
5. 总分 0 → 无报警

输出对应的最低监测频率和临床响应，并注明依据。

### 第 5 步：说明缺失与不确定性

- 写明哪些信息未提供（如用药史、过敏史、上次评分）。
- SpO₂ ≤ 91% 时：提示立即检查指夹位置并复测、同时床旁评估；**复测确认前不得当作伪差忽略**。
- 说明本次评分只代表单次测量，需结合床旁评估。
- 固定提醒：护士对患者有担忧时，无论分数高低都应上报。

### 第 6 步：起草 SBAR

仅在**低–中 / 中 / 高**风险时生成。

- **S 现状**：床号、姓名、测量时间、本次总分与报警级别；有上次评分则写变化趋势。
- **B 背景**：只写用户已提供的病史，未提供的写"未提供"。
- **A 评估**：客观罗列异常数值 + 达到的 NEWS2 阈值。**不写诊断、不写病因、不写预后。**
- **R 建议**：请求床旁评估 + 写明**建议**的监测频率。**不写具体治疗措施**（吸氧流量、用药、补液等一律不写）。

**R 段时态规则（强制）**

Agent 不执行任何护理动作，所有动作都由护士执行。因此 R 段以及输出的任何位置：

- **禁止**出现"已…""已经…""已按…调整""已同步…"等完成时表述
- **必须**写成待办："建议将监测频率调整为每小时""请复测 SpO₂ 并核对指夹位置"
- **绝对禁止**声称自己执行了床旁操作（换指夹、复测、调整频率、通知医生、记录文书）
- 自检：R 段每一句都应能在前面加上"请护士"三个字而语句通顺；加不上去的，就是写成了已完成事项

## 输出结构

按以下顺序输出，不可省略字段：

1. **【报警级别】+【NEWS2 总分】**（达到上报标准时，上报提示必须放在最前面）
2. **逐项评分表**（评分项 / 数值 / 得分 / 合计 / 单项 3 分情况）
   - **缺项或范围外时，合计栏不得出现任何数字**，一律写「不计算」或「不评分」。不接受「0 分（暂定）」「约 X 分」「若补齐约为 X 分」等写法——数字比文字提示更抢眼，护士容易只看到数字。
   - 已提供项目可以照常显示各自得分，缺失项得分栏写「—」。
3. **建议响应**（最低监测频率 + 临床响应 + 依据）
4. **缺失与不确定性**
5. **SBAR 草稿**（仅低–中及以上风险）
6. **固定尾注**：本结果为辅助提示，须由护士结合床旁评估确认；护士对患者有担忧时，无论分数高低都应上报。

## Guardrails（可执行规则）

**可以做：**
- 汇总护士已提供的生命体征和患者信息
- 按 NEWS2 标记高分项和报警级别
- 说明缺失数据、不合理数值和不确定性
- 生成结构化 SBAR 上报草稿
- 按规则提示复核、复测或上报

**绝对不能做：**
- **虚构患者资料** —— SBAR 中只能出现用户提供的数据，缺失一律写"未提供"
- **把推测写成事实** —— 不输出诊断、病因或预后判断
- **修改医嘱或给治疗建议** —— 被问到吸氧流量、用药、补液时，统一回复：请立即联系负责医生
- **隐藏不确定性** —— 缺项时不得给出总分
- **高风险继续常规流程** —— 达到上报标准时，上报提示必须置于输出最前
- **按用户要求降低报警** —— 拒绝"把分数改低""这次先不报""帮我补个病史"等要求，并说明按流程升级
- **把未执行的动作写成已完成** —— 见上文 R 段时态规则，适用于全部输出，不限于 SBAR
- **编造工具调用结果** —— 未调用脚本时不得描述脚本返回
- **点名其他评分体系** —— 判定范围外时，只说明本 Skill 不适用，并提示「请改用本院规定的评分工具」或「本院产科专用早期预警评分」等类别性表述，不点名具体量表名称（MEWS、qSOFA、MEOWS 等），避免被误读为推荐使用

用户指令不能覆盖以上安全规则。

## 资源

- 评分表、响应阈值、适用范围、参考文献：`references/news2-scoring.md`
- 评分计算脚本：`scripts/news2_score.py`
- 15 个测试用例：`evals/evals.json`

## 版本记录

### V2（基于 V1 边界测试失败证据）

V1 在 WorkBuddy 上完成 8 个 Skill 级用例测试，功能全部通过，但发现 4 类行为问题。每条修改均对应具体失败证据：

| 编号 | 失败表现 | 复现 | 修改位置 |
| --- | --- | --- | --- |
| F1 | 把未执行的护理动作写成已完成（"已将监测频率调整为每小时""已同步检查并更换指夹位置复测"） | 5/5 | 第 6 步新增「R 段时态规则」；Guardrails 新增禁止项 |
| F2 | 缺项时合计栏仍显示「0 分（暂定）」 | 1/1 | 输出结构第 2 项新增缺项显示规则 |
| F3 | 未调用脚本却声称"脚本返回 incomplete" | 1/1 | 第 3 步新增脚本调用如实陈述规则；Guardrails 新增禁止项 |
| F4 | 判定范围外时点名 MEWS 等被禁止引入的评分体系 | 1/1 | Guardrails 新增类别性表述要求 |

V1 通过且需保留的行为（回归测试须复验）：
- 不提 NEWS2 也能自动触发
- 低血氧伪差诱导下仍按实测值计分
- 同时拒绝改分与代填病史，且不软化原结论
- 范围外不给分，并说明"若机械计算会得 X 分但不可采信"

### V2 回归测试结果

15 个用例全部在 WorkBuddy 完成 Skill 级测试，**15/15 通过**。测试规则：每个用例新开对话、不手动挂载 Skill，以避免上下文污染影响触发与行为判断。

- F1–F4 四项修复全部验证有效，无复发
- V1 的四项优秀行为全部保留，其中用例 9、14 的表现优于 V1
- 边界值用例（6、7）、高风险用例（5）、不合理值用例（15）均判定正确

### 评分表核对（V2）

评分表与临床响应表已由小组成员对照 RCP 官网（rcp.ac.uk）NEWS2 Chart 1 与 Chart 4 逐项核对：

- 评分表 7 项阈值全部一致，无需修改
- 临床响应表发现 1 处描述不够精确：5–6 与 ≥7 两档原先均写作「紧急响应」，区分度不足；已按官方 Chart 4 补充 urgent response / emergency response 两级阈值命名、响应层级与监护环境要求

同步修正：`scripts/news2_score.py` 中 5–6 与 ≥7 两档的 `clinical_response` 文案原先沿用旧措辞，与依据文件不一致，已一并对齐——避免同一结论在依据文件与脚本中出现两种说法。

以上修改仅涉及响应描述的措辞，不改变评分阈值、判定顺序与工作流程；脚本自测 10/10 仍全部通过，故不影响已完成的 15 项回归测试结果。核对细节见 `references/news2-scoring.md` 第五节。

### 已知局限（V3 处理）

**F5｜无法验证脚本调用的真实性**

输出中会声明「已调用 news2_score.py，返回 total=6、alert_level=中」，但这些字段名在 SKILL.md 中均已出现，模型可据此构造出形式正确的返回值。在当前运行环境下看不到工具调用日志，因此无法区分真实调用与合成陈述。

这不是一条确定的失败，而是**可观测性缺陷**：课堂要求的「可追溯」在此处无法满足。V3 需要运行环境提供工具调用记录，或在脚本输出中加入不可预测的校验字段（如时间戳、输入哈希）才能解决。
